<style>
    
    .counter {
        z-index: 1;
        width: 600px;
        height: 500px;
        margin-left: -300px;
        position: relative;
        left: 50%;
        top: 50px;
        background-color: lightgray;
        text-align: center;
        color: white;
        font-family: sans-serif;
        box-shadow: 7px 7px 5px black;
    }
    
    p {
        font-size: 70px;
        position: relative;
        top: 40px;
        font-weight: 500;
    }
    
    #button2 {
        display: block;
        background-color: rgb(23, 23, 145);
        width: 160px;
        font-size: 18px;
        height: 30px;
        margin-left: -80px;
        position: relative;
        top: 50px;
        left: 50%;
        color: white;
        border-radius: 7px;
        box-shadow: 2px 2px 5px black;
    }
    
    #button1 {
        display: block;
        background-color: rgb(23, 23, 145);
        width: 160px;
        font-size: 18px;
        height: 30px;
        margin-left: -80px;
        position: relative;
        top: 35px;
        left: 50%;
        color: white;
        border-radius: 7px;
        box-shadow: 2px 2px 5px black;
    }
    
    h1 {
        position: relative;
        top: 40px;
        color: white;
        font-size: 48px;
        font-family: monospace;
    }
    .HexColor{
    text-align:center;
   margin:0 auto;
}
</style>
<section class="Jscode">
<div class="container">
<div class="counter">
    {% if section.settings.headline !=blank %}
    <h1>{{section.settings.headline }}</h1>
    {% endif %}
    <p id="para"></p>
    <button id="button1" onclick="bu1()">Add Count</button>
    <button id="button2">Lower Count</button>
  </div>
  <div class="HexColor">
  <h2>Hex Color:<span id="hex"></span></h2>
  <button onclick="changeColor()">Change color hexically 🙂</button>
</div>
</div>
</section>
<script>
   var no = document.getElementById('para');
   no.innerText = 0

    function numberCounter() {
        if ((no.innerText) > 0) {
            no.style.color = "green"
        } else if ((no.innerText) < 0) {
            no.style.color = "red"
        } else if ((no.innerText) == 0) {
            no.style.color = "white"
        }
    }
    document.getElementById('button1').addEventListener("click", function() {
        no.innerText = Number(no.innerText) + Number(1)
        numberCounter()
    })

    document.getElementById('button2').addEventListener("click", function() {
            no.innerText =Number(no.innerText) - Number(1)
        numberCounter()
    })

    var body = document.getElementById('Jscode').style
        
    function makeid(length) {
        var result = '#';
        var characters = 'ABCDEF123456789';
        var charactersLength = characters.length;
        for (var i = 0; i < length; i++) {
            result += characters.charAt(Math.floor(Math.random() * charactersLength));
        }
        return (result);
    } 

    function changeColor() {
        const text = document.getElementById('hex')
        const color = makeid(6)

        text.innerText = color
        body.backgroundColor = color
    }
</script>
{% schema %}
{
  "name": "Jscode",
  "settings": [
    {
      "type": "text",
      "id": "headline",
      "label": "headline",
      "default": "Counter"
    },
 
  ],
  "presets": [{ "name": "Jscode" }]
}
{% endschema %}
